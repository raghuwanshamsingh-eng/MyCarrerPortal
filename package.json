{
  "name": "mycareerportal-root",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "ai-services/*"
  ],
  "scripts": {
    "start": "npm run dev --workspace frontend",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend || true"
  }
}
